# Terraform local artifacts (do NOT commit)
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Local vars (secrets) - never commit
*.tfvars
*.auto.tfvars
*.tfvars.json

# Plans
*.plan
*.bin

# OS/Editor
.DS_Store
*.swp
*.swo

# Keep the lock file pinned in git
!.terraform.lock.hcl
EOF
